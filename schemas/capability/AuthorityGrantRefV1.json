{
  "$id": "https://widgetdc.com/contracts/capability/AuthorityGrantRefV1.json",
  "additionalProperties": false,
  "description": "Inert reference to possible separately issued authority. It contains no grant, policy decision, permission, or signature.",
  "type": "object",
  "required": [
    "schema_version",
    "definition_version",
    "canonicalization_profile",
    "hash_algorithm",
    "canonical_document_hash",
    "issuer",
    "grant_id",
    "scope_ref",
    "expires_at",
    "reference_version",
    "reference_only"
  ],
  "properties": {
    "schema_version": {
      "const": "wdc.authority_grant_ref.v1",
      "type": "string"
    },
    "definition_version": {
      "const": "1.0.0",
      "type": "string"
    },
    "canonicalization_profile": {
      "const": "jcs-rfc8785-v1",
      "type": "string"
    },
    "hash_algorithm": {
      "const": "sha256",
      "type": "string"
    },
    "canonical_document_hash": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "description": "SHA-256 identity over the canonical contract projection, excluding this field itself.",
      "type": "string"
    },
    "issuer": {
      "minLength": 1,
      "maxLength": 512,
      "type": "string"
    },
    "grant_id": {
      "minLength": 1,
      "maxLength": 512,
      "type": "string"
    },
    "scope_ref": {
      "minLength": 1,
      "maxLength": 512,
      "type": "string"
    },
    "expires_at": {
      "pattern": "^[0-9]{4}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12][0-9]|3[01])T(?:[01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](?:\\.[0-9]+)?(?:Z|[+-](?:[01][0-9]|2[0-3]):[0-5][0-9])$",
      "description": "RFC 3339-shaped wire string with T separator and an explicit Z or numeric UTC offset. Calendar and expiry-policy evaluation are outside this inert-reference contract.",
      "type": "string"
    },
    "reference_version": {
      "pattern": "^[1-9][0-9]*(?:\\.[0-9]+){0,2}$",
      "maxLength": 64,
      "type": "string"
    },
    "reference_only": {
      "const": true,
      "type": "boolean"
    }
  }
}
