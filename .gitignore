node_modules/
scripts/__pycache__/
python/**/__pycache__/
python/**/*.pyc
arch/.ast_graph.db

# Architecture graph outputs (generated, ephemeral)
# Exception: platform-graph.json is committed for Railway MCP server
arch/*.json
!arch/platform-graph.json
!arch/data-sources.json

# dist/ is committed — required for git+https: consumers (npm strips source via `files` field)
# Regenerate: npm run build
# schemas/ and python/ are also committed — generated artifacts consumed by other repos
# Do NOT add dist/, schemas/, or python/ here
